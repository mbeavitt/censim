[tool:pytest]
minversion = 6.0
addopts = -ra -q --tb=short
testpaths =
    test_reproducibility.py
markers =
    performance: marks tests as performance tests (deselect with '-m "not performance"')
    regression: marks tests as regression tests against reference data (deselect with '-m "not regression"')
timeout = 300